# Notas de la tarea

Este archivo existe desde el inicio para demostrar la operación **leer**.

- El servidor de sistema de archivos solo puede operar dentro de esta carpeta (`sandbox/`).
- Todo lo que está fuera de `sandbox/` debe ser rechazado por el servidor.
